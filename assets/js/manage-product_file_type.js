window.addEventListener('DOMContentLoaded', function(){
	jQuery(function($){
		// $('#tag-slug').prop('disabled', true);
		// $('#slug').prop('disabled', true);

		// $(document).on('click', '.row-actions .editinline', function(e){
		// 	let $this = $(this);
		// 	setTimeout(function(){
		// 		let $row = $this.closest('tr');
		// 		$('tr#edit-'+$row.find('input[name="delete_tags[]"]').val()).find('input[name="slug"]').prop('disabled', true).closest('label').hide();
		// 	}, 50);
		// });
	});
});